#include "peripherals.h"

int stopi(char * string, int length);		//converts the given str to positive int
int pitos(int num, char* string);		//converts the given +ve int to str
float stopf(char * string_conv, int length);	//converts the given str to a positive float
int StrLength(char * string);	//Finds length of a string

